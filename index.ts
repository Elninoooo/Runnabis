import { registerRootComponent } from 'expo';

import App from './App';

/**
 * Point d'entrée de l'application
 *
 * Pour voir les composants dans Storybook :
 * Lancer `npm run storybook` et ouvrir http://localhost:6006
 */
registerRootComponent(App);
